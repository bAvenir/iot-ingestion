[project]
name = "iot-ingestion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JozefZiduliak", email = "Jozef.Ziduliak@bavenir.eu" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
iot-ingestion = "iot_ingestion:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
